use schooldatabase;
CREATE TABLE department (
dept_name VARCHAR(20),
location VARCHAR(20),
annual_budget INTEGER(6),
PRIMARY KEY (dept_name)
);

Insert into department values 
('Computing', 'South' '100,000');
